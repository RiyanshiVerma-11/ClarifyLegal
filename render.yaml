services:
  - type: web
    name: clarifylegal
    env: node
    plan: free
    region: oregon
    buildCommand: bun install && bun run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
